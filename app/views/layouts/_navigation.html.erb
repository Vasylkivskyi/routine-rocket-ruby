<div class="navbar bg-base-100">
  <div class="flex-1">
    <%= link_to '🚀 Routin Rocket', root_path, class: "btn btn-ghost text-xl" %>
  </div>
  
  <div class="flex-none">
    <ul class="menu menu-horizontal px-1">
      <% if user_signed_in? %>
        <li>
          <details>
            <summary>My Habits</summary>
            <ul class="bg-base-100 rounded-t-none p-2 z-2 relative">
              <li><%= link_to "Create", new_habit_path %></li>
              <li><%= link_to "Review", habits_path %></li>
            </ul>
          </details>
        </li>
      <% end %>
      
      <% if !user_signed_in? %>
        <li><%= link_to "Sign In", new_user_session_path %></li>
        <li><%= link_to "Sign Up", new_user_registration_path %></li>
      <% end %>
    </ul>

    <% if user_signed_in? %>
      <div class="dropdown dropdown-end">
        <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar">
          <div class="w-10 rounded-full">
            <img alt="Avatar" src="https://img.daisyui.com/images/stock/photo-1534528741775-53994a69daeb.webp" />
          </div>
        </div>

        <ul tabindex="0" class="menu menu-sm dropdown-content bg-base-100 rounded-box z-2 relative mt-3 w-52 p-2 shadow">
          <li>
            <%= link_to "Profile", edit_user_registration_path(current_user) %>
          </li>
          <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
        </ul>
      </div>
    <% end %>
  </div>
</div>
